import { FlatList, StyleSheet, Text, View } from "react-native";

const Header = () => {
  return (
    <View style={ styles.header }>
      <Text style={ sty }>My Todos</Text>
    </View>
  );
}

let styles = StyleSheet.create( {

} )
 
export default Header;